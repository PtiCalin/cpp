# Visual Studio Notes

- Create a Console App project.
- Add `.h` and `.cpp` files by feature.
- Keep class declaration in header, method definitions in source files.
- Build with **Build Solution**.
- Run with **Ctrl+F5** to keep console visible after execution.
